package Planes;

public class NotSpecifiedPlane extends Plane {
    public NotSpecifiedPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
